const theaterReducer = (state = {theaters: [], loading: false}, action) => {
        switch(action.type) {
            default:
                return state; 
        }
}

export default theaterReducer